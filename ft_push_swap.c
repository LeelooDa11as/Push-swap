#include "ft_push_swap.h"

int	main(int argc, char *argv[])
{
	//no argument, return 0
	if (argc == 1)
		return (0);
	//check if arguments are numbers in int range and no duplicates
	if (ft_check_num(argc,argv) == 1) // is cases its checks everything
		//push_swap() recibira el stack a(completo) y b


	
	//cheakear numeros y en caso de error enviarlos por el fd de error
	return (1);
}

